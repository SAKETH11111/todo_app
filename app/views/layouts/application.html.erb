<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Todo App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js"></script>
  </head>

  <body class="min-h-screen" data-controller="theme">
    <header class="px-4 sm:px-5 lg:px-6 py-4 border-b border-[var(--border)]">
      <div class="flex items-center justify-between max-w-7xl mx-auto">
        <div class="text-sm text-[var(--muted-foreground)]">
          Welcome back Chad
        </div>
        <div class="flex items-center gap-4">
          <button class="p-2 hover:bg-[var(--muted)] rounded-lg transition-colors" aria-label="Search">
            <iconify-icon icon="mdi:magnify" class="w-5 h-5"></iconify-icon>
          </button>
          <button class="p-2 hover:bg-[var(--muted)] rounded-lg transition-colors" aria-label="Notifications">
            <iconify-icon icon="mdi:bell-outline" class="w-5 h-5"></iconify-icon>
          </button>
        </div>
      </div>
    </header>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
